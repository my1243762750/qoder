spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mini_jira_test?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: minijira
    password: my@123456
  jpa:
    hibernate:
      ddl-auto: create-drop  # 测试环境每次都重建表
    show-sql: true
  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false

security:
  jwt:
    secret: test-secret-key-for-jwt-token-signing-must-be-at-least-256-bits
    validity-ms: 3600000

logging:
  level:
    root: WARN
    com.qoder.minijira: DEBUG
