# 贡献指南

感谢你对 Mini Jira 项目的关注！

## 🤝 如何贡献

### 报告 Bug

如果你发现了 Bug，请：

1. 在 Issues 中搜索，确认该问题是否已被报告
2. 如果没有，创建新的 Issue，包含：
   - Bug 的详细描述
   - 复现步骤
   - 期望行为
   - 实际行为
   - 环境信息（操作系统、Java 版本等）
   - 相关日志或截图

### 建议新功能

1. 创建 Issue，标题以 `[Feature Request]` 开头
2. 详细描述功能需求和使用场景
3. 说明为什么需要这个功能

### 提交代码

1. **Fork 项目**
   ```bash
   # Fork 后克隆到本地
   git clone https://github.com/your-username/mini-jira.git
   cd mini-jira
   ```

2. **创建分支**
   ```bash
   # 功能分支
   git checkout -b feature/your-feature-name
   
   # 修复分支
   git checkout -b fix/issue-number-description
   ```

3. **编写代码**
   - 遵循项目代码规范
   - 添加必要的测试
   - 更新相关文档

4. **提交变更**
   ```bash
   git add .
   git commit -m "type: description"
   ```
   
   提交类型：
   - `feat`: 新功能
   - `fix`: Bug 修复
   - `docs`: 文档更新
   - `style`: 代码格式调整
   - `refactor`: 重构
   - `test`: 测试相关
   - `chore`: 构建/工具链更新

5. **推送到 GitHub**
   ```bash
   git push origin feature/your-feature-name
   ```

6. **创建 Pull Request**
   - 在 GitHub 上创建 PR
   - 填写 PR 模板
   - 等待 Review

## 📝 代码规范

### Java 代码规范

- 遵循 Java 命名规范
- 类名使用 PascalCase
- 方法和变量使用 camelCase
- 常量使用 UPPER_CASE
- 每个类、方法添加必要注释

### API 设计规范

- 使用 RESTful 风格
- 统一响应格式：`{ code, message, data }`
- 错误码规范：
  - `0` - 成功
  - `1000` - 参数错误
  - `2000` - 认证错误
  - `3000` - 业务错误
  - `5000` - 系统错误

### Git 提交规范

```
type(scope): subject

body

footer
```

示例：
```
feat(user): add user registration API

- Add RegisterRequest DTO
- Add UserService.register method
- Add validation for email and username

Closes #123
```

## 🧪 测试要求

- 所有新功能必须包含单元测试
- 测试覆盖率不低于 80%
- 集成测试需覆盖主要业务流程

运行测试：
```bash
mvn test
```

## 📚 文档要求

- API 变更需更新 README.md
- 新功能需更新 CHANGELOG.md
- 复杂逻辑需添加代码注释

## ❓ 需要帮助？

- 阅读 [README.md](README.md)
- 查看 [学习计划](backend-learning-plan.md)
- 在 Issues 中提问
- 联系维护者

## 📜 行为准则

- 尊重所有贡献者
- 建设性反馈
- 保持友好和专业

---

再次感谢你的贡献！🎉
